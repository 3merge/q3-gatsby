const loadContent = require('./loadContent');
const {
  appendSiblingsToContext,
  paginateArchiveContext,
} = require('./pagination');

module.exports = {
  loadContent,
  appendSiblingsToContext,
  paginateArchiveContext,
};
